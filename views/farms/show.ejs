<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>farmApp | <%= farm.name %>の詳細ページ</title>
  </head>
  <body>
    <h1><%= farm.name %></h1>
    <ul>
      <li>市: <%= farm.city %></li>
      <li>メールアドレス: <%= farm.email %></li>
    </ul>
    <h2>商品一覧</h2>
    <ul>
      <% for(let product of farm.products) { %>
      <li><a href="/products/<%= product._id %>"><%= product.name %></a></li>
      <% } %>
    </ul>
    <a href="/farms/<%= farm._id %>/products/new">商品追加</a>
    <a href="/farms">農場一覧へ戻る</a>
  </body>
</html>
